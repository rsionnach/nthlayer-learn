# SQLite Store

Default verdict store. Single file, zero dependencies.

A year of verdicts from a small deployment (100 verdicts/day) is ~150 MB.

## Status

Implementation priority: **Phase 2** (after schema and Python core library).
