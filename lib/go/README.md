# Verdict Go Library

Go implementation of the verdict transport library. Same operations as the Python library: create, link, resolve, supersede, query, accuracy.

## Status

Implementation priority: **Phase 5** (after Python library, CLI, and OTel conventions).
