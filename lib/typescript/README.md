# Verdict TypeScript Library

TypeScript implementation of the verdict transport library. Same operations as the Python library: create, link, resolve, supersede, query, accuracy.

## Status

Implementation priority: **Phase 6** (after Go library).
